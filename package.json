{
  "name": "youtube-chinese-subtitles",
  "version": "1.1.5",
  "private": true,
  "description": "Chinese and bilingual subtitles on the YouTube player, plus a timestamped transcript, AI overviews, and notes.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "bash scripts/check-release.sh",
    "package": "bash scripts/package-extension.sh"
  }
}
